let currentMC=50;
function mcRun(n){currentMC=n;document.querySelectorAll('.mc-btn').forEach(b=>b.classList.remove('active'));const btn=document.getElementById('mc'+n);if(btn)btn.classList.add('active');const canvas=document.getElementById('mcCanvas'),ctx=canvas.getContext('2d'),dpr=window.devicePixelRatio||1,rect=canvas.getBoundingClientRect();canvas.width=rect.width*dpr;canvas.height=rect.height*dpr;ctx.scale(dpr,dpr);const W=rect.width,H=rect.height;const S0=100,mu=.08,sigma=.25,T=1,steps=252,dt=T/steps;const isDark=document.documentElement.getAttribute('data-theme')==='dark';ctx.clearRect(0,0,W,H);ctx.strokeStyle=isDark?'rgba(59,130,246,.06)':'rgba(37,99,235,.06)';ctx.lineWidth=.5;for(let i=0;i<=4;i++){const y=i*H/4;ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}let allPaths=[];for(let p=0;p<n;p++){let path=[S0],s=S0;for(let i=0;i<steps;i++){const z=Math.sqrt(-2*Math.log(Math.random()))*Math.cos(2*Math.PI*Math.random());s*=Math.exp((mu-.5*sigma*sigma)*dt+sigma*Math.sqrt(dt)*z);path.push(s)}allPaths.push(path)}const allVals=allPaths.flat(),mn=Math.min(...allVals)*.95,mx=Math.max(...allVals)*1.05;const colors=['rgba(59,130,246,','rgba(167,139,250,','rgba(232,121,168,','rgba(245,158,66,','rgba(34,197,94,'];allPaths.forEach((path,pi)=>{const c=colors[pi%colors.length];ctx.beginPath();ctx.strokeStyle=c+(n>100?'0.08)':'0.25)');ctx.lineWidth=n>100?.5:1;path.forEach((v,i)=>{const x=(i/steps)*W,y=H-((v-mn)/(mx-mn))*H;i===0?ctx.moveTo(x,y):ctx.lineTo(x,y)});ctx.stroke()});ctx.beginPath();ctx.strokeStyle=isDark?'#3b82f6':'#2563eb';ctx.lineWidth=2;for(let i=0;i<=steps;i++){const avg=allPaths.reduce((s,p)=>s+p[i],0)/n;const x=(i/steps)*W,y=H-((avg-mn)/(mx-mn))*H;i===0?ctx.moveTo(x,y):ctx.lineTo(x,y)}ctx.stroke()}
